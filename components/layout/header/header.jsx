import React from "react"

export default function Header(props) {

    return (
        <header id="header" className="flex bg-primary transition-colors justify-between h-header fixed w-full top-0 shadow-xl ml-0">

        </header>
    )
}
